@extends('layouts.app')
@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">{{ __('Dashboard') }}</div>

                    <div class="card-body">
                        @if (session('status'))
                            <div class="alert alert-success" role="alert">
                                {{ session('status') }}
                            </div>
                        @endif
                        <div>
                            @foreach(\App\Models\Article::whereHas('items')->get() as $article)
                                <a href="{{ action([\App\Http\Controllers\OrderController::class, 'stepOne'], $article->id) }}"
                                   class="btn btn-primary">
                                    {{ $article->title }}
                                </a>
                            @endforeach
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
